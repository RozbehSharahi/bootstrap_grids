#!/usr/bin/env bash

set -e

# Usage example from project root (requires `app` container already running or run `./.docker/bin/up` first):
#   ./.docker/bin/composer --version
#   ./.docker/bin/composer update
.docker/bin/compose exec app composer "$@"
